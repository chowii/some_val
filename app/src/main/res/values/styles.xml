<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="switchStyle">@style/AppSwitch</item>

        <!--<item name="android:textColorPrimary">@color/white</item>-->
        <!--<item name="actionMenuTextColor">@color/white</item>-->

        <!-- The style for the widgets on the ActionBar. -->
        <!--<item name="actionBarWidgetTheme">@style/WidgetStyle</item>-->
        <item name="colorControlNormal">@color/white</item>
    </style>

    <style name="WidgetStyle" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorControlNormal">@color/white</item>
    </style>
    <!--<item name="tabMaxWidth">@dimen/tab_max_width</item>-->
    <!--<item name="tabSelectedTextColor">@color/range</item>-->
    <!--<item name="tabIndicatorColor">?attr/colorAccent</item>-->
    <style name="EvaluationTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@android:color/transparent</item>
        <item name="tabBackground">@drawable/bg_main_tab_selector</item>
        <item name="tabTextAppearance">@style/MainAppTabTextAppearance</item>
    </style>

    <!-- for text -->
    <!--<item name="android:textSize">12sp</item>-->
    <style name="MainAppTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textColor">@color/text_color_tab_main</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="AppSwitch">
        <item name="android:background">@drawable/bg_switch</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="AppTextInputEditText" parent="Widget.AppCompat.EditText">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="DisabledAppTextInputLayout" parent="AppTextInputEditText">
        <item name="android:enabled">false</item>
    </style>

    <style name="AppBadge">
        <item name="android:background">@drawable/bg_badge</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">16sp</item>
        <item name="android:layout_width">@dimen/badge_diameter</item>
        <item name="android:layout_height">@dimen/badge_diameter</item>
    </style>

    <style name="TextVehicleDetails">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">14sp</item>
        <item name="android:layout_marginTop">4dp</item>
    </style>

    <style name="TextVehicleDetailsTitle">
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="DialogFlatButton" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textAllCaps">true</item>
    </style>

    <style name="MyButton" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/a50_white</item>
    </style>
</resources>
